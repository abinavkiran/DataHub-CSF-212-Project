# Module 1: DAG Architecture & Schema Design
**Owner:** Abinav Kiran

## Responsibilities
- Implementing SQLAlchemy models for `Commit`, `Tree`, `TreeEntry`, `Blob`, `Metadata`.
- Writing correct Postgres schemas and migrations.
- **Strictly No API or Routing logic.** Use `docker-compose run dev-env pytest` to test schemas.
