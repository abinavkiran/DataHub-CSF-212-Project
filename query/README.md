# Module 6: Query Language & Reporting
**Owner:** Pashuvula Niranand Reddy

## Responsibilities
- Produce AST or query objects from string input (e.g. `datahub log --metric "accuracy > 0.9"`).
- Read-only execution; strict protection against SQL injection.
