# Module 5: Metadata Extraction & Indexing
**Owner:** Saurabh Kumar

## Responsibilities
- Automatically parse CSV/JSON/Parquet files to index metadata.
- Handle corrupted files cleanly (return `{}` instead of crashing server).
