[pytest]
minversion = 7.0
addopts = -ra -q
# Discover tests inside these module directories
testpaths =
    infrastructure/tests
    storage/tests
    cli/tests
    api/tests
    metadata/tests
    query/tests
